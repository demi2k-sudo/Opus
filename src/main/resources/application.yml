spring:
  datasource:
    url: jdbc:postgresql://pg-11dd0389-nabothdemetrius-b0f7.h.aivencloud.com:12609/defaultdb?sslmode=require
    username: avnadmin
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use 'validate' in production
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8048

jwt:
  secret: "super-secret-key-should-be-long-and-random"
  expiration-ms: 86400000